class Solution(object):
    def removeDuplicates(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        seens = []
        for i in nums:
            if i in seens:
                continue
            else:
                seens.append(i)
        nums[:] = seens
        return len(nums)