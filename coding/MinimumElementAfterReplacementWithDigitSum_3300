class Solution:
    def minElement(self, nums: List[int]) -> int:
        ans = 9999
        for i in nums:
            tem = 0
            while i > 0:
                tem += i % 10
                i //= 10
            if tem <= ans:
                ans = tem
        return ans
